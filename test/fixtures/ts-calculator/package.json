{
  "name": "ts-calculator",
  "version": "1.0.0",
  "description": "A simple TypeScript calculator for testing",
  "main": "index.js",
  "scripts": {
    "test": "vitest run",
    "build": "tsup src/index.ts --format esm,cjs",
    "dev": "tsup src/index.ts --format esm,cjs --watch",
    "lint": "eslint src"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^0.34.0",
    "tsup": "^8.0.0",
    "eslint": "^8.0.0"
  }
}
